import React, { Component } from 'react';
import HomePage from './HomePage';

export default class RoomJoinPage extends Component {
    constructor(props){
        super(props);
    }

    render(){

        return <p>this is room join HomePage</p>
    }
}